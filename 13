from datetime import datetime

print("----------------------------------")
print("--         BIENVENIDO A        ---")
print("----LA TIENDA DE MASCOTAS --------")
print("----------------------------------")

inventario = {
     "perro": 4,
     "gato": 3,
     "pajaro": 10,
     "iguana": 5 
}

total = 0
for val in inventario.values():
     total += val

print("Ingrese su nombre")
nombre = input()
print("Ingrese su apellido")
apellido = input()

#Concatenacion
nombre_completo = nombre + " " + apellido

print("Gracias por venir", nombre_completo)

compras = []

def mostrar_menu():

    print("Selecciona una opcion")
    print("1: Saber la cantidad de animales")
    print("2 Comprar un animal")
    print("3:Mostrar Compras")
    print("4:Salir de la tienda")
   
def mostrar_inventario():
    

        print("****** INVENTARIO *******")
        for llave, valor in inventario.items():
             print(f"  {llave}: {valor}")
             
       
        print("En total hay:", total)

def comprar_animal():
    carrito = []

    while True:

        print("Que animal deseas? Solo uno por especie")
        print("Escribe E para salir o V para ver tu carrito")
        animal = input()
        
        if animal == "E": break
             
        if animal == "V":
             print(f"Tu carrito contiene{carrito}")
             continue
        
        if animal not in inventario:
             print(f"Lo siento no contamos con el animal {animal}")
        elif inventario[animal] == 0:
             print(f"Lo sentimos no tenemos en exixtencia el animal {animal}")
        
        elif animal not in carrito:
            carrito.append(animal)
        
        else:
             print("Ese animal ya lo tienes en tu carrito")
        #print("Has comprado", animal)

    print("El contenido del carrito es")
    for animal in carrito:
         print("   ", animal)
         inventario[animal] -= 1

         #Agregar esta compra al carrito de compras
         fecha = datetime.now()
         compras.append((nombre, carrito, fecha))

def mostrar_compras():
     print("")
     print("****** COMPRAS REALIZADAS *****")
     for compra in compras:
          print(f"  {compra[0]} compro {compra[1]} en {compra[2]}")

while True:
    mostrar_menu()
    respuesta = int(input())
    
    if respuesta == 1:
         mostrar_inventario()
    elif respuesta == 2:
        comprar_animal()
    elif respuesta == 3:
         mostrar_compras()
   
    elif respuesta == 4:
        print("Nos vemos")
        break
